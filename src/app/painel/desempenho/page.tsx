import Link from"next/link";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{formatarBRL}from"@/lib/pricing";import{Cabecalho,Kpi,wrap}from"../shared";import{salvarMeta}from"./actions";
export default async function DesempenhoPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const{data:p}=await supabase.from("profiles").select("role,avatar_url").eq("id",user.id).single();if(p?.role!=="profissional")redirect("/painel");const since=new Date();since.setDate(1);since.setHours(0,0,0,0);const [{data:targets},{data:quotes},{data:jobs},{data:orders},{data:goal},{data:pro}]=await Promise.all([supabase.from("quote_request_targets").select("quote_request_id,visto_em,recusado_em").eq("professional_id",user.id).gte("enviado_em",since.toISOString()),supabase.from("quotes").select("quote_request_id,status").eq("professional_id",user.id).gte("created_at",since.toISOString()),supabase.from("jobs").select("id,status").eq("profissional_id",user.id).gte("created_at",since.toISOString()),supabase.from("orders").select("preco_servico,comissao_servico,payment_status,created_at").gte("created_at",since.toISOString()),supabase.from("professional_goals").select("revenue_target").eq("professional_id",user.id).eq("month",since.toISOString().slice(0,7)+"-01").maybeSingle(),supabase.from("professionals").select("bio,anos_experiencia,banner_url,professional_skills(id),service_areas(id),portfolio_items(id)").eq("id",user.id).maybeSingle()]);const sent=targets?.length??0,responses=quotes?.length??0,wins=(quotes??[]).filter(q=>q.status==="aceita").length,completed=(jobs??[]).filter(j=>["concluido","avaliado"].includes(j.status)).length;const revenue=(orders??[]).filter(o=>o.payment_status==="pago").reduce((s,o)=>s+Number(o.preco_servico)-Number(o.comissao_servico),0);const target=Number(goal?.revenue_target??0);const completeness=[p.avatar_url,pro?.banner_url,pro?.bio,(pro?.anos_experiencia??0)>0,(pro?.professional_skills?.length??0)>0,(pro?.service_areas?.length??0)>0,(pro?.portfolio_items?.length??0)>0].filter(Boolean).length/7;const suggestions:string[]=[];if(!pro?.bio)suggestions.push("Escreva uma bio objetiva explicando especialidade e diferencial.");if(!(pro?.portfolio_items?.length))suggestions.push("Adicione fotos reais de antes e depois ao portfólio.");if(sent>=5&&responses/sent<.6)suggestions.push("Responda oportunidades mais cedo e explique o escopo da proposta.");if(responses>=5&&wins/responses<.2)suggestions.push("Revise garantia, itens incluídos e clareza da proposta — não apenas o preço.");return <div style={wrap}><Cabecalho eyebrow="Gestão" titulo="Desempenho"/><p style={{color:"var(--ink-soft)"}}>Funil do mês com o mesmo período em todas as etapas. Sem vaidade e sem denominadores misturados.</p><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(145px,1fr))",gap:10}}><Kpi label="Oportunidades" valor={String(sent)}/><Kpi label="Propostas" valor={String(responses)} sufixo={sent?`${Math.round(responses/sent*100)}% das oportunidades`:"sem amostra"}/><Kpi label="Ganhos" valor={String(wins)} sufixo={responses?`${Math.round(wins/responses*100)}% das propostas`:"sem amostra"}/><Kpi label="Concluídos" valor={String(completed)}/></div><section className="card" style={{padding:22,marginTop:16}}><h2 style={{fontSize:17}}>Meta do mês</h2><strong style={{fontSize:24}}>{formatarBRL(revenue)}</strong>{target>0&&<p>{Math.min(100,Math.round(revenue/target*100))}% de {formatarBRL(target)}</p>}<form action={salvarMeta} style={{display:"flex",gap:8,flexWrap:"wrap"}}><input type="hidden" name="month" value={since.toISOString().slice(0,7)}/><input name="target" type="number" min="1" step="0.01" defaultValue={target||undefined} placeholder="Meta de receita líquida" style={{padding:10,border:"1px solid var(--line)",borderRadius:9}}/><button className="btn">Salvar meta</button></form></section><section className="card" style={{padding:22,marginTop:16}}><h2 style={{fontSize:17}}>Assistente de perfil</h2><p>Completude: <strong>{Math.round(completeness*100)}%</strong></p>{suggestions.length?<ul>{suggestions.map(s=><li key={s} style={{marginBottom:8}}>{s}</li>)}</ul>:<p style={{color:"var(--good)"}}>Seu perfil cobre os principais sinais de confiança. Continue atualizando trabalhos reais.</p>}<Link href="/painel/perfil" className="btn">Melhorar meu perfil</Link><p style={{fontSize:12,color:"var(--ink-faint)"}}>Visualizações de perfil não aparecem porque esse evento ainda não é instrumentado; não exibimos estimativas inventadas.</p></section></div>}
