"use server"; import {revalidatePath} from "next/cache"; import {createClient} from "@/lib/supabase/server";
export async function salvarNota(formData:FormData){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return;const customer_id=String(formData.get("customerId")??"");const notes=String(formData.get("notes")??"").trim();if(!customer_id||!notes)return;await supabase.from("professional_client_notes").upsert({professional_id:user.id,customer_id,notes},{onConflict:"professional_id,customer_id"});revalidatePath("/painel/clientes");}
