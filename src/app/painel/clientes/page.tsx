import {redirect} from "next/navigation"; import {createClient} from "@/lib/supabase/server"; import {Cabecalho,wrap} from "../shared"; import {salvarNota} from "./actions";
export default async function ClientesPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const{data:profile}=await supabase.from("profiles").select("role").eq("id",user.id).single();if(profile?.role!=="profissional")redirect("/painel");const[{data:jobs},{data:notes},{data:equipment}]=await Promise.all([supabase.from("jobs").select("cliente_id,created_at,status,cliente:profiles!jobs_cliente_id_fkey(nome)").eq("profissional_id",user.id).order("created_at",{ascending:false}),supabase.from("professional_client_notes").select("customer_id,notes").eq("professional_id",user.id),supabase.from("customer_equipment").select("id,customer_id,brand,model,capacity_btu")]);const by=new Map<string,{name:string;count:number;last:string}>();for(const job of jobs??[]){const c=Array.isArray(job.cliente)?job.cliente[0]:job.cliente;const old=by.get(job.cliente_id);by.set(job.cliente_id,{name:c?.nome??"Cliente",count:(old?.count??0)+1,last:old?.last??job.created_at});}const noteMap=new Map((notes??[]).map(n=>[n.customer_id,n.notes]));return <div style={wrap}><Cabecalho eyebrow="Relacionamento" titulo="Meus clientes"/><p style={{color:"var(--ink-soft)"}}>Contexto privado para atender melhor quem já confia no seu trabalho.</p><div style={{display:"grid",gap:12}}>{[...by.entries()].map(([id,c])=><article className="card" key={id} style={{padding:20}}><h2 style={{fontSize:17}}>{c.name}</h2><p style={{color:"var(--ink-faint)"}}>{c.count} serviço(s) · último contato {new Date(c.last).toLocaleDateString("pt-BR")}</p><p>{(equipment??[]).filter(e=>e.customer_id===id).map(e=>[e.brand,e.model].filter(Boolean).join(" ")).filter(Boolean).join(", ")||"Nenhum equipamento cadastrado pelo cliente"}</p><form action={salvarNota}><input type="hidden" name="customerId" value={id}/><textarea name="notes" defaultValue={noteMap.get(id)??""} required maxLength={4000} placeholder="Nota privada sobre acesso, preferências e contexto…" rows={3} style={{width:"100%",padding:10,border:"1px solid var(--line)",borderRadius:9}}/><button className="btn" style={{marginTop:8}}>Salvar nota privada</button></form></article>)}</div></div>}
