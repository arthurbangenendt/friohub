import Link from "next/link"; import {redirect} from "next/navigation"; import {createClient} from "@/lib/supabase/server"; import {Cabecalho,wrap} from "../shared"; import {EquipmentForm} from "./EquipmentForm";
export default async function EquipamentosPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const{data:items}=await supabase.from("customer_equipment").select("id,brand,model,capacity_btu,installed_at,site:customer_sites(label,address),maintenance_recommendations(due_on,reason,status)").eq("customer_id",user.id).order("created_at",{ascending:false});return <div style={wrap}><Cabecalho eyebrow="Seu patrimônio" titulo="Equipamentos"/><p style={{color:"var(--ink-soft)"}}>Histórico por aparelho, garantia e manutenção no mesmo lugar.</p><EquipmentForm/><div style={{display:"grid",gap:12,marginTop:18}}>{(items??[]).map((item)=>{const site=Array.isArray(item.site)?item.site[0]:item.site;const due=(item.maintenance_recommendations??[]).find((m)=>m.status==="recommended");return <article key={item.id} className="card" style={{padding:20}}><h2 style={{fontSize:17}}>{[item.brand,item.model].filter(Boolean).join(" ")||"Ar-condicionado"}</h2><p style={{color:"var(--ink-soft)"}}>{site?.label} · {site?.address}{item.capacity_btu?` · ${item.capacity_btu.toLocaleString("pt-BR")} BTU`:""}</p>{due&&<p style={{color:"var(--warm)"}}>Manutenção recomendada em {new Date(`${due.due_on}T12:00:00`).toLocaleDateString("pt-BR")}: {due.reason}</p>}<Link href={`/solicitar?equipment=${item.id}`} className="btn">Solicitar novo atendimento</Link></article>})}{!items?.length&&<p className="card" style={{padding:20,color:"var(--ink-faint)"}}>Cadastre seu primeiro aparelho para construir um histórico útil.</p>}</div></div>}
